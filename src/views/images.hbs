<!-- Swiper -->
  <div class="swiper-container">
    <div class="swiper-wrapper">
        {{#each photos}}
        <img src="{{this.imageUrl}}" alt="" class="swiper-slide">
        {{/each}}
    </div>
    <!-- Add Pagination -->
    <div class="swiper-pagination"></div>
  </div>

  


